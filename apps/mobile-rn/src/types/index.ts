/**
 * Re-export all shared types for the RN app from the shared monorepo package.
 * Metro is configured to resolve '@margwatch/shared-types' to the local package.
 */
export * from '@margwatch/shared-types';

